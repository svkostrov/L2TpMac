# L2TP Office — L2TP-клиент для macOS (без IPsec)

Нативное macOS-приложение (SwiftUI, окно + значок в menu bar) для подключения к чистому L2TP-серверу **без IPsec** — например, к офисному туннелю, аналогичному интерфейсу `L2TP0` на Keenetic. Внутри используется `pppd` для PPP-интерфейса и встроенный user-space L2TP helper; системные VPN-профили macOS и глобальный `/etc/ppp/options` не меняются.

<img src="L2TPOfficeApp/AppIcon_1024.png" width="96" alt="Иконка L2TP Office">

## Зачем, если в macOS есть VPN

Штатный клиент macOS умеет только **L2TP over IPsec** и требует shared secret. Если на сервере IPsec нет (`security-level public` на Keenetic, чистый L2TP на Cisco ISR и т.п.), системным интерфейсом подключиться нельзя. Это приложение решает именно такой случай.

## Возможности

- Подключение/отключение в один клик из окна или из menu bar (замок в строке меню: закрыт — туннель активен).
- Рабочий режим — **split-туннель**: в туннель заворачиваются только указанные CIDR-сети (по умолчанию `172.16.99.0/24`), остальной трафик и DNS идут напрямую.
- Настройки подключения переживают перезапуск приложения.
- Пароль VPN можно временно показать/скрыть в поле ввода.
- Автозапуск при входе в систему и автоподключение при старте приложения.
- Живой лог PPP/L2TP прямо в окне (`/tmp/l2tp-office-app.log`, pppd с `debug` + сообщения helper).
- Права root — через ограниченный root-helper. При первом подключении приложение один раз попросит пароль администратора, установит helper в `/Library/PrivilegedHelperTools` и sudoers-правило только для него; последующие подключения/отключения идут без повторного системного пароля.
- Защита от чужих PPP-туннелей: приложение управляет только своими pppd (маркер в argv) и не убивает сторонние.
- Восстановление сети после отключения: очистка стейл-записей сетевого стора, восстановление default route и DNS (см. BR-18 в [bugreports.md](bugreports.md)).
- При выходе из приложения активный туннель корректно отключается перед завершением.

## Установка

1. Скачай `L2TP-Office-<версия>.zip` из [Releases](https://github.com/svkostrov/L2TpMac/releases), распакуй, перенеси `L2TP Office.app` в `/Applications`.
2. Подпись ad-hoc, поэтому при первом запуске: ПКМ по приложению → «Открыть» → «Открыть» (или `xattr -cr "/Applications/L2TP Office.app"`).
3. macOS 13+; приложение собирается universal для Apple Silicon и Intel.

При первом запуске укажи сервер, логин, пароль и сети для split-туннеля, затем нажми «Подключить» — появится системный запрос пароля администратора.

## Сборка из исходников

```bash
git clone https://github.com/svkostrov/L2TpMac.git
cd L2TpMac
./build-install.command   # компиляция, сборка бандла, подпись, установка в /Applications
```

Скрипт собирает `L2TPOfficeApp/main.swift` и `L2TPOfficeHelper/main.go`, кладёт иконку и Info.plist, подписывает ad-hoc **после** копирования ресурсов (иначе ломается подпись и Dock показывает белый квадрат), устанавливает в `/Applications` и сбрасывает кэш иконок.

## CLI-скрипты (без GUI)

В репозитории есть и консольный вариант:

```bash
sudo ./l2tp-connect.sh      # подключить
sudo ./l2tp-disconnect.sh   # отключить
ifconfig ppp0               # статус
```

Лог: `/tmp/l2tp-office.log`. Пароль в скрипте хранится открытым текстом — правь под себя и не выкладывай.

## Технические заметки

**User-space L2TP backend.** Начиная с версии 1.13 приложение не использует Apple `L2TP.ppp`: `pppd` запускается поверх `pty`, а отдельный helper внутри `.app` сам устанавливает UDP/L2TPv2-сессию и мостит PPP-кадры. Это сделано, чтобы не трогать системный L2TP over IPsec и обойти проблему, где ICMP через Apple L2TP-плагин работал, а TCP зависал в `SYN_SENT`.

**Соответствие опций Keenetic → pppd:**

| Keenetic | pppd |
|---|---|
| `peer <ip>` | `remoteaddress` |
| `authentication identity/password` | `user` / `password` |
| `lcp echo 30 3` | `lcp-echo-interval 30`, `lcp-echo-failure 3` |
| `no ccp` | `noccp` |
| `ipcp default-route` | `defaultroute` |
| `ipcp dns-routes` | `usepeerdns` |
| `ip mtu 1400` | `mtu 1200`, `mru 1200` |

**Восстановление сети (BR-18).** Если pppd умирает, не откатив закоммиченный сетевой стор, configd продолжает считать мёртвый PPP-сервис основным: default route деградирует до interface-scoped, глобальный DNS пустеет — интернет пропадает. Скрипты подключения и отключения чистят стейл-ключи `State:/Network/Service/*/IPv4`, восстанавливают default route (с повторами) и DNS через обновление DHCP-аренды. Подробности — в [bugreports.md](bugreports.md).

## Известные ограничения

- В split-режиме DNS-имена офисной сети не резолвятся (DNS сервера не используется) — обращайся по IP.

## QA

Список найденных и исправленных багов, замечаний и результаты тест-сессий — в [bugreports.md](bugreports.md). Кратко по последней сессии (v1.2): подключение в обоих режимах, split-маршрутизация, отключение и полное восстановление сети (маршруты, DNS, интернет) — OK; непрерывный фоновый ping пережил все циклы подключений/отключений без единой потери пакета.

Локальные автотесты:

```bash
./tests/run-tests.sh
```

## История версий

- **1.54** — добавлены source-regression тесты для GUI-регрессий: полный лог, menu bar action-кнопка, обычный стиль «Настроек», запрет дубликатов окна через New Window.
- **1.53** — окно теперь показывает весь PPP/L2TP-лог текущей сессии без ограничения последними строками; menu bar-панель получила одну контекстную кнопку подключения/отключения рядом со статусом, а «Настройки» перенесены ниже и больше не выделяются синим.
- **1.52** — окно теперь показывает весь PPP/L2TP-лог текущей сессии с момента запуска приложения, без ограничения последними строками.
- **1.51** — улучшены подсказки в форме, предупреждение об устаревшем root-helper, обновление root-helper при локальной сборке, менее агрессивное автопереподключение, cleanup opts-файла с паролем, точнее AUTHFAIL и стабильнее UDP retry в helper-е.
- **1.50** — добавлены автотесты для L2TP helper/root-helper, сборка стала universal (arm64 + x86_64), исправлено устаревшее SwiftUI API в лог-скролле.
- **1.49** — лог PPP/L2TP больше не очищается при переподключениях; он сохраняется от запуска приложения до закрытия и очищается только при свежем старте приложения или кнопкой «Очистить лог».
- **1.48** — при автоперестройке туннеля в PPP/L2TP-лог добавляется англоязычная причина: смена default internet path или потеря ping до PPP remote IP.
- **1.47** — автопереподключение стало быстрее: приложение перестраивает туннель после двух подряд неудачных ping до PPP remote IP или при изменении системного пути к интернету, например после поднятия внешнего VPN.
- **1.46** — в меню-баре добавлен live ping до PPP remote IP текущего подключения; значение обновляется в фоне каждые 5 секунд.
- **1.45** — в меню-баре ссылка GitHub перенесена справа от блока версии/проверки обновлений, а основная кнопка окна переименована в «Настройки».
- **1.44** — ручная проверка обновлений больше не показывает отдельный диалог, если установлена актуальная версия; статус остаётся в блоке обновлений. Также обновлено меню-бар меню: выразительная кнопка открытия окна, ссылка на GitHub и «Выход» внизу.
- **1.43** — меню-бар приведён к более современному виду: выразительная кнопка открытия окна, ссылка на GitHub, аккуратный блок обновлений и «Выход» внизу меню.
- **1.42** — служебная строка об успешном поднятии туннеля в PPP/L2TP-логе переведена на английский, чтобы не выбиваться из общего формата pppd.
- **1.41** — в лог добавлена явная строка об успешном поднятии туннеля с локальным IP, PPP-сервером и списком сетей через VPN.
- **1.40** — в меню-баре блок проверки обновлений совмещён с отображением текущей версии, как в основном окне приложения.
- **1.39** — кнопка «Отключить» теперь отображается красным цветом, как действие остановки/разрыва подключения.
- **1.38** — маршрут до L2TP-сервера теперь фиксируется через текущий системный путь к интернету, включая активные внешние VPN-туннели; добавлена опция автопереподключения при обрыве.
- **1.37** — исправлено подключение при активном внешнем `utun`-туннеле: транспорт L2TP до сервера направляется через активный туннель, если он перехватывает default-трафик.
- **1.36** — в интерфейсе оставлен только режим выбранных сетей; кнопки «Отключить» и «Остановить подключение» объединены в одну динамическую кнопку; статус обновлений больше не дублирует номер текущей версии.
- **1.35** — блок проверки обновлений перенесён в верхнюю строку справа от статуса подключения: там отображаются текущая версия и статус проверки/установки обновлений.
- **1.34** — из UI и README убраны технические детали хранения VPN-пароля; поле пароля теперь подписано нейтрально.
- **1.33** — устранён системный диалог доступа к конфиденциальной информации после обновлений; пароль нужно ввести заново один раз.
- **1.32** — усилено восстановление сети при отключении: root-helper удаляет split-маршруты, очищает PPP-состояние SystemConfiguration, возвращает default route через активный `en*`, восстанавливает DNS через DHCP/router fallback и повторно проверяет маршрут после cleanup.
- **1.31** — добавлена кнопка «Очистить лог»; при запуске приложение очищает лог локально без запроса пароля, а ручная очистка выполняется через root-helper и работает с root-owned логом.
- **1.30** — добавлены повторные попытки UDP-send/reconnect в L2TP helper при transient-ошибках macOS `can't assign requested address`, `network is unreachable`, `no route to host` во время старта из `pppd pty`.
- **1.29** — исправлена отправка UDP в user-space L2TP helper: вместо wildcard `0.0.0.0` используется connected UDP socket, чтобы macOS корректно выбирала исходный адрес и не падала с `can't assign requested address`.
- **1.28** — исправлен регресс подключения `sendto: network is unreachable`: при новом подключении root-helper больше не сбрасывает DHCP/маршруты как при полном disconnect, ждёт маршрут до VPN-сервера и проверяет версию установленного helper-а при обновлении приложения.
- **1.27** — добавлена аварийная кнопка «Остановить подключение» в основном окне и менюбаре: она доступна даже при зависшем состоянии подключения и принудительно запускает cleanup через root-helper.
- **1.26** — исправлено отключение туннеля после обновления со старых версий: приложение теперь проверяет уже установленный root-helper пробным запуском через `sudo -n`, а не по правам текущего пользователя.
- **1.25** — при выходе/Cmd+Q приложение сначала корректно отключает свой активный туннель через root-helper и только потом завершается.
- **1.24** — добавлена кнопка показать/скрыть пароль VPN в поле пароля; также включает root-helper с одноразовой установкой из 1.23.
- **1.23** — добавлен root-helper с одноразовой установкой: системный пароль нужен только для первичной установки helper-а, затем connect/disconnect выполняются без повторного запроса пароля через ограниченное sudoers-правило.
- **1.22** — подсеть split-туннеля по умолчанию изменена на `172.16.99.0/24`; прежняя шаблонная настройка автоматически заменяется на новую.
- **1.21** — учтены оба варианта root-аутентификации: сначала Touch ID для уже настроенного `sudo`, при недоступности — fallback на обычный системный пароль; приложение больше не пытается менять `/etc/pam.d/sudo_local`.
- **1.20** — исправлена первичная настройка Touch ID для sudo: setup-скрипт создаётся во временной директории и уже под root устанавливает `/etc/pam.d/sudo_local`.
- **1.19** — root-действия переведены с парольного `osascript with administrator privileges` на `sudo` с Touch ID через `pam_tid.so`; пароль администратора может потребоваться только один раз для первичной настройки `/etc/pam.d/sudo_local`.
- **1.18** — helper принудительно использует IPv4 UDP socket (`udp4`, `0.0.0.0:0`), чтобы macOS не открывала `[::]` socket и не падала с `sendto: can't assign requested address`.
- **1.17** — сохранённые настройки маршрутизации принудительно переводятся в split-режим; усилено восстановление default route/DNS после отключения.
- **1.16** — helper переведён с connected UDP на unconnected UDP socket, чтобы macOS выбирала актуальный source address и не падала с `can't assign requested address`.
- **1.15** — ручная маршрутизация: `pppd defaultroute/usepeerdns` не используются, DNS macOS не трогается.
- **1.14** — исправлен запуск user-space backend: ошибки раннего старта `pppd` попадают в лог окна.
- **1.13** — внедрён несистемный L2TPv2 helper: `pppd` работает через `pty`, Apple `L2TP.ppp` и `/etc/ppp/options` больше не используются для основного подключения.
- **1.12** — откат от системного L2TP service: приложение снова не создаёт/не меняет macOS VPN-профили и готовится к изолированному user-space L2TP backend.
- **1.8** — для L2TP/PPP отключены VJ TCP header compression и PPP field compression, добавлены более строгие PPP-опции для TCP через туннель.
- **1.7** — в приложении отображается только номер версии, без номера build.
- **1.6** — сетевой фикс: маршрут до VPN-сервера закрепляется через обычный шлюз, split-маршруты добавляются через PPP peer, MTU/MRU снижены до 1200.
- **1.5** — добавлена проверка обновлений через GitHub Releases: автоматом при запуске и вручную из меню.
- **1.4** — версия в меню перенесена ниже кнопок «Открыть окно»/«Выход», подпись выровнена по центру.
- **1.3** — номер версии отображается в главном окне и меню приложения.
- **1.2** — исправлен BR-18 (потеря интернета после отключения): очистка стейл-стора configd, восстановление default route и DNS; зачистка стора и перед подключением.
- **1.1** — надёжное «Открыть окно» из menu bar (показ существующего окна вместо молчаливого отказа `openWindow`); скрипты сборки и публикации.
- **1.0** — первая версия: GUI, два режима маршрутизации, автозапуск, live-лог.
